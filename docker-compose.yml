version: "3.8"

services:
  backend:
    build: ./irony
    container_name: irony_backend
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    # networks:
    #   - app_network
  frontend:
    build:
      context: ./irony-service-agent-ui
      args:
        NEXT_PUBLIC_API_URL: "http://backend:8000"
    container_name: irony_frontend
    ports:
      - "3000:3000"

    depends_on:
      - backend

    environment:
      - NEXT_PUBLIC_API_URL=http://backend:8000

    restart: unless-stopped
    # networks:
    #   - app_network
# networks:
#   app_network:
#     driver: bridge
